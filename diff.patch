diff --git a/data_analysis/ai/engine.py b/data_analysis/ai/engine.py
index b62ef30..7596316 100644
--- a/data_analysis/ai/engine.py
+++ b/data_analysis/ai/engine.py
@@ -27,6 +27,7 @@ def _get_logger(logger: Any | None = None) -> Any:
         return logging.getLogger(__name__)
 
 from data_analysis.core.lazy_imports import get_genai  # noqa: E402
+import contextlib
 
 # ---------------------------------------------------------------------------
 # Module-level mutable state (owned here, not in runtime_app)
@@ -183,7 +184,7 @@ def _extract_text_from_gemini_response(resp: Any, *, logger: Any | None = None)
 
 def _make_model(name: str, *, logger: Any | None = None) -> Any:
     genai = get_genai()
-    model_ctor = getattr(genai, "GenerativeModel")
+    model_ctor = genai.GenerativeModel
     m = model_ctor(name)
 
     resp = m.generate_content(
@@ -290,22 +291,20 @@ def configure_ai(*, logger: Any | None = None) -> None:
             api_key = os.getenv("GOOGLE_API_KEY")
             if api_key:
                 try:
-                    getattr(genai, "configure")(api_key=api_key)
+                    genai.configure(api_key=api_key)
                 except Exception as e:
                     logger = _get_logger(logger)
                     if logger:
                         logger.warning("First configure attempt failed, retrying: %s", e)
-                    getattr(genai, "configure")(api_key=api_key)
+                    genai.configure(api_key=api_key)
 
                 AI_ENABLED = True
                 model = None
                 logger = _get_logger(logger)
                 if logger:
                     logger.info("AI configured successfully using API key.")
-                try:
+                with contextlib.suppress(Exception):
                     _set_ai_status("OK", ready=False, configured=True, model_name=None)
-                except Exception:
-                    pass
                 return
 
             credentials_path = os.getenv("GOOGLE_APPLICATION_CREDENTIALS")
@@ -316,7 +315,7 @@ def configure_ai(*, logger: Any | None = None) -> None:
                     if f.endswith('.json'):
                         json_path = os.path.join(scan_dir, f)
                         try:
-                            with open(json_path, 'r', encoding='utf-8') as jf:
+                            with open(json_path, encoding='utf-8') as jf:
                                 content = json.load(jf)
                                 if content.get('type') == 'service_account':
                                     credentials_path = json_path
@@ -324,7 +323,7 @@ def configure_ai(*, logger: Any | None = None) -> None:
                                     if logger:
                                         logger.info("Found service account JSON: %s", json_path)
                                     break
-                        except (json.JSONDecodeError, KeyError, IOError):
+                        except (OSError, json.JSONDecodeError, KeyError):
                             continue
 
             google_auth_available = False
@@ -344,16 +343,14 @@ def configure_ai(*, logger: Any | None = None) -> None:
                     credentials = _sa.Credentials.from_service_account_file(
                         credentials_path, scopes=scopes
                     )
-                    getattr(genai, "configure")(credentials=credentials)
+                    genai.configure(credentials=credentials)
                     AI_ENABLED = True
                     model = None
                     logger = _get_logger(logger)
                     if logger:
                         logger.info("AI configured successfully using service account credentials.")
-                    try:
+                    with contextlib.suppress(Exception):
                         _set_ai_status("OK (Service Account)", ready=False, configured=True, model_name=None)
-                    except Exception:
-                        pass
                     return
                 except Exception as e:
                     logger = _get_logger(logger)
@@ -369,10 +366,8 @@ def configure_ai(*, logger: Any | None = None) -> None:
                 logger.exception("AI configuration failed")
             model = None
             AI_ENABLED = False
-            try:
+            with contextlib.suppress(Exception):
                 _set_ai_status("Configuration failed", ready=False, configured=False, model_name=None)
-            except Exception:
-                pass
 
 
 def ensure_ai_ready(*, logger: Any | None = None) -> bool:
@@ -387,10 +382,8 @@ def ensure_ai_ready(*, logger: Any | None = None) -> bool:
                 configure_ai(logger=logger)
 
             if not AI_ENABLED:
-                try:
+                with contextlib.suppress(Exception):
                     _set_ai_status("AI disabled or not configured.", ready=False)
-                except Exception:
-                    pass
                 return False
             if model is None:
                 try:
@@ -399,25 +392,19 @@ def ensure_ai_ready(*, logger: Any | None = None) -> bool:
                     logger = _get_logger(logger)
                     if logger:
                         logger.warning("Lazy model init failed: %s", e)
-                    try:
+                    with contextlib.suppress(Exception):
                         _set_ai_status(str(e), ready=False, model_name=None)
-                    except Exception:
-                        pass
                     return False
-            try:
+            with contextlib.suppress(Exception):
                 _set_ai_status("OK", ready=True, model_name=CURRENT_MODEL_NAME)
-            except Exception:
-                pass
             return True
     except Exception as e:
         try:
             logger = _get_logger(logger)
             if logger:
                 logger.warning("ensure_ai_ready failed: %s", e)
-            try:
+            with contextlib.suppress(Exception):
                 _set_ai_status(str(e), ready=False)
-            except Exception:
-                pass
         except Exception:
             pass
         return False
diff --git a/data_analysis/ai/service.py b/data_analysis/ai/service.py
index 9c855b4..733c453 100644
--- a/data_analysis/ai/service.py
+++ b/data_analysis/ai/service.py
@@ -9,12 +9,14 @@ from __future__ import annotations
 
 import os
 import re
-from typing import Any, Callable
+from typing import Any
+from collections.abc import Callable
 
 import pandas as pd
 
 from data_analysis.ai import engine as ai_engine
 from data_analysis.ai import html_format as ai_html
+import contextlib
 
 # ---------------------------------------------------------------------------
 # Helpers
@@ -242,10 +244,8 @@ def get_ai_summary_with_file(
             if logger:
                 logger.info("Summary continuation skipped: %s", ce)
 
-        try:
+        with contextlib.suppress(Exception):
             text = _attach_model_marker(text)
-        except Exception:
-            pass
 
         sanitized = sanitize(text)
 
@@ -483,7 +483,7 @@ def get_or_cache_ai_summary_for(
             path = os.path.join(uploads_dir, f"{safe_name}.ai_summary.html")
             if os.path.exists(path):
                 try:
-                    with open(path, 'r', encoding='utf-8') as f:
+                    with open(path, encoding='utf-8') as f:
                         disk_cached = f.read()
                 except Exception:
                     pass
@@ -544,7 +544,7 @@ def _get_clean_ai_summary_from_cache(
             path = os.path.join(uploads_dir, f"{safe_name}.ai_summary.html")
             if os.path.exists(path):
                 try:
-                    with open(path, 'r', encoding='utf-8') as f:
+                    with open(path, encoding='utf-8') as f:
                         cached = f.read()
                 except Exception:
                     pass
diff --git a/data_analysis/analysis/anomaly.py b/data_analysis/analysis/anomaly.py
index 01374e7..18dec0e 100644
--- a/data_analysis/analysis/anomaly.py
+++ b/data_analysis/analysis/anomaly.py
@@ -1,6 +1,7 @@
 from __future__ import annotations
 
-from typing import Any, Callable
+from typing import Any
+from collections.abc import Callable
 
 import numpy as np
 import pandas as pd
diff --git a/data_analysis/analysis/context.py b/data_analysis/analysis/context.py
index 462ce0b..9208b24 100644
--- a/data_analysis/analysis/context.py
+++ b/data_analysis/analysis/context.py
@@ -3,7 +3,8 @@ from __future__ import annotations
 import io
 import json
 import html as htmllib
-from typing import Any, Callable
+from typing import Any
+from collections.abc import Callable
 
 import numpy as np
 import pandas as pd
diff --git a/data_analysis/analysis/dataframe_ops.py b/data_analysis/analysis/dataframe_ops.py
index bb156aa..0ffe653 100644
--- a/data_analysis/analysis/dataframe_ops.py
+++ b/data_analysis/analysis/dataframe_ops.py
@@ -4,9 +4,11 @@ import json
 import os
 import time
 from datetime import datetime, timedelta
-from typing import Any, Callable, Iterable
+from typing import Any
+from collections.abc import Callable, Iterable
 
 import pandas as pd
+import contextlib
 
 SUPPORTED_ENCODINGS = ["utf-8", "utf-8-sig", "cp1252", "latin1"]
 
@@ -14,7 +16,7 @@ SUPPORTED_ENCODINGS = ["utf-8", "utf-8-sig", "cp1252", "latin1"]
 def _load_name_map(name_map_path: str, logger: Any | None = None) -> dict[str, Any] | None:
     try:
         if os.path.exists(name_map_path):
-            with open(name_map_path, "r", encoding="utf-8") as handle:
+            with open(name_map_path, encoding="utf-8") as handle:
                 loaded = json.load(handle)
             return loaded if isinstance(loaded, dict) else {}
     except Exception as exc:
@@ -110,23 +112,23 @@ def read_json_fallback(path: str, *, supported_encodings: Iterable[str] | None =
     encodings = list(supported_encodings or SUPPORTED_ENCODINGS)
     for enc in encodings:
         try:
-            with open(path, "r", encoding=enc, errors="strict") as handle:
+            with open(path, encoding=enc, errors="strict") as handle:
                 return pd.read_json(handle, orient="records")
         except UnicodeDecodeError as exc:
             last_err = exc
             continue
         except ValueError:
             try:
-                with open(path, "r", encoding=enc, errors="strict") as handle:
+                with open(path, encoding=enc, errors="strict") as handle:
                     return pd.read_json(handle, lines=True)
             except Exception:
                 continue
     try:
-        with open(path, "r", encoding="utf-8", errors="replace") as handle:
+        with open(path, encoding="utf-8", errors="replace") as handle:
             return pd.read_json(handle, orient="records")
     except ValueError:
         try:
-            with open(path, "r", encoding="utf-8", errors="replace") as handle:
+            with open(path, encoding="utf-8", errors="replace") as handle:
                 return pd.read_json(handle, lines=True)
         except Exception:
             pass
@@ -153,10 +155,8 @@ def read_excel_smart(path: str) -> pd.DataFrame:
                                             df[cand] = dt
                                     except Exception:
                                         pass
-                                try:
+                                with contextlib.suppress(Exception):
                                     df = df.set_index(cand)
-                                except Exception:
-                                    pass
                                 break
                         else:
                             first_col = df.columns[0]
@@ -347,10 +347,8 @@ def get_dataframe_for(
                 logger.info("get_dataframe_for: reader returned non-DataFrame for %s", filename)
             return None
 
-        try:
+        with contextlib.suppress(Exception):
             df = df.dropna(axis=1, how="all")
-        except Exception:
-            pass
 
         try:
             if isinstance(df.index, pd.DatetimeIndex) and len(df.index) >= 20:
@@ -436,10 +434,8 @@ def get_dataframe_for(
                     # Do not drop the column so it remains available in tables
                     df.index = ts
                     df.index.name = picked
-                    try:
+                    with contextlib.suppress(Exception):
                         df = df[ts.notna()].sort_index()
-                    except Exception:
-                        pass
         except Exception as exc:
             if logger is not None:
                 logger.debug("get_dataframe_for: datetime inference skipped: %s", exc)
diff --git a/data_analysis/analysis/forecast.py b/data_analysis/analysis/forecast.py
index 60e30a5..fee6b7e 100644
--- a/data_analysis/analysis/forecast.py
+++ b/data_analysis/analysis/forecast.py
@@ -3,6 +3,7 @@
 from __future__ import annotations
 
 from data_analysis.runtime_app import *
+import contextlib
 
 _LOCAL_SYMBOLS = {
     '_LOCAL_SYMBOLS',
@@ -180,10 +181,8 @@ def _infer_future_index(idx, steps):
             offset = pd.Timedelta(hours=1)
         
         # Debug logging
-        try:
+        with contextlib.suppress(Exception):
             app.logger.debug("Forecast: %d steps, offset=%s, last_date=%s, forecast_end=%s", steps, offset, idx[-1], idx[-1] + offset * steps)
-        except Exception:
-            pass
         
         # Generate future timestamps manually to ensure correct spacing
         start = idx[-1]
@@ -208,9 +207,7 @@ def _infer_seasonal_period(idx, min_seasons=2):
     f = freq.upper()
     if f.startswith("H"):
         period = 24
-    elif f.startswith("T") or f.startswith("MIN"):
-        period = 60
-    elif f.startswith("S"):
+    elif f.startswith("T") or f.startswith("MIN") or f.startswith("S"):
         period = 60
     elif f.startswith("D"):
         period = 7
diff --git a/data_analysis/analysis/plot.py b/data_analysis/analysis/plot.py
index afa2aac..00b700d 100644
--- a/data_analysis/analysis/plot.py
+++ b/data_analysis/analysis/plot.py
@@ -11,6 +11,7 @@ import matplotlib.ticker as mticker
 import numpy as np
 import pandas as pd
 from matplotlib.transforms import blended_transform_factory
+import contextlib
 
 
 # ---------------------------------------------------------------------------
@@ -575,10 +576,8 @@ def generate_forecast_plot(
     ax.grid(True, alpha=0.3)
 
     # Keep headroom/side room so value tags can stay fully visible.
-    try:
+    with contextlib.suppress(Exception):
         ax.margins(x=0.06)
-    except Exception:
-        pass
     
     # Improve X-axis readability
     try:
@@ -683,10 +682,8 @@ def generate_forecast_plot(
         ax.plot([], [], color='#94a3b8', linestyle=':', label=f'Std: {_format_stat_value(hist_std)}')
 
         # Reserve space for the right-side Avg/Med label lane.
-        try:
+        with contextlib.suppress(Exception):
             fig.subplots_adjust(right=0.84)
-        except Exception:
-            pass
 
         # Legend on single line - below x-axis title (Index)
         legend_anchor = -0.30 if legend_y is None else legend_y
@@ -957,10 +954,8 @@ def generate_correlation_heatmap(df, method='spearman', title='Correlation Heatm
     except Exception as e:
         if fig is not None:
             plt.close(fig)
-        try:
+        with contextlib.suppress(Exception):
             rt.app.logger.debug("generate_correlation_heatmap failed (%s): %s", method, e)
-        except Exception:
-            pass
         return None
 
 
diff --git a/data_analysis/core/lazy_imports.py b/data_analysis/core/lazy_imports.py
index 56985c3..1178194 100644
--- a/data_analysis/core/lazy_imports.py
+++ b/data_analysis/core/lazy_imports.py
@@ -93,7 +93,7 @@ def get_isolation_forest() -> Any:
         with _SKLEARN_LOCK:
             if _ISOLATION_FOREST is None:
                 module = importlib.import_module("sklearn.ensemble")
-                _ISOLATION_FOREST = getattr(module, "IsolationForest")
+                _ISOLATION_FOREST = module.IsolationForest
     return _ISOLATION_FOREST
 
 
@@ -104,7 +104,7 @@ def get_stl() -> Any:
         with _STL_LOCK:
             if _STL_CLASS is None:
                 module = importlib.import_module("statsmodels.tsa.seasonal")
-                _STL_CLASS = getattr(module, "STL")
+                _STL_CLASS = module.STL
     return _STL_CLASS
 
 
diff --git a/data_analysis/middleware.py b/data_analysis/middleware.py
index a0b7fb0..167d0d9 100644
--- a/data_analysis/middleware.py
+++ b/data_analysis/middleware.py
@@ -1,7 +1,8 @@
 from __future__ import annotations
 
 import os
-from typing import Any, Callable
+from typing import Any
+from collections.abc import Callable
 
 
 _HEADERS_MIDDLEWARE_KEY = "data_analysis_headers_middleware_registered"
diff --git a/data_analysis/reports/pdf_report.py b/data_analysis/reports/pdf_report.py
index 30ee9d9..360680c 100644
--- a/data_analysis/reports/pdf_report.py
+++ b/data_analysis/reports/pdf_report.py
@@ -716,7 +716,7 @@ def handle_download_full_report_pdf(filename):
                     pdf.set_font(default_font, '', font_size_int)  # data is regular
                     max_rows_for_width = 150
                     data_widths: list[float] = []
-                    for col_pos, col_name in enumerate(chunk_headers):
+                    for col_pos, _ in enumerate(chunk_headers):
                         source_col = chunk_cols[col_pos]
                         series_values = [str(v) for v in df_display[source_col].iloc[:max_rows_for_width]]
                         max_data_w = max((pdf.get_string_width(v) for v in series_values), default=0.0) + 2.0
diff --git a/data_analysis/routes/api.py b/data_analysis/routes/api.py
index b3209a1..1884b1e 100644
--- a/data_analysis/routes/api.py
+++ b/data_analysis/routes/api.py
@@ -399,7 +399,7 @@ def handle_api_interactive_data(filename):
         extent_int = int(max(1, total_x_extent))
         if extent_int > tick_count:
             raw_ticks = np.linspace(0, extent_int - 1, num=tick_count, dtype=float).tolist()
-            tv_num = sorted(set(int(round(v)) for v in raw_ticks))
+            tv_num = sorted({int(round(v)) for v in raw_ticks})
             if tv_num[-1] != extent_int - 1:
                 tv_num.append(extent_int - 1)
         else:
diff --git a/data_analysis/routes/upload.py b/data_analysis/routes/upload.py
index d987454..55317e0 100644
--- a/data_analysis/routes/upload.py
+++ b/data_analysis/routes/upload.py
@@ -72,7 +72,7 @@ def handle_upload_file():
                 try:
                     size_bytes = os.path.getsize(final_path)
                     if size_bytes <= app.config['AI_FULL_UPLOAD_MAX_MB'] * 1024 * 1024:
-                        uploaded = getattr(_get_genai(), "upload_file")(
+                        uploaded = _get_genai().upload_file(
                             path=final_path,
                             mime_type="text/csv",
                             display_name=orig_name,
diff --git a/data_analysis/runtime_app.py b/data_analysis/runtime_app.py
index 416420a..a274c38 100644
--- a/data_analysis/runtime_app.py
+++ b/data_analysis/runtime_app.py
@@ -863,7 +863,7 @@ if __name__ == "__main__":
     rate_limit = os.getenv("RATE_LIMIT")
     if Limiter and rate_limit:
         try:
-            limiter = Limiter(get_remote_address, app=app, default_limits=[rate_limit])
+            Limiter(get_remote_address, app=app, default_limits=[rate_limit])
             app.logger.info("Rate limiting enabled: %s", rate_limit)
         except Exception as e:
             app.logger.warning("Limiter init failed: %s", e)
diff --git a/tests/unit/test_app.py b/tests/unit/test_app.py
index c820633..865cbff 100644
--- a/tests/unit/test_app.py
+++ b/tests/unit/test_app.py
@@ -1175,7 +1175,7 @@ def test_api_interactive_anomalies_snap_to_displayed_history_points(monkeypatch)
     traces = first.get("traces", [])
     hist = next(t for t in traces if str(t.get("name", "")).lower() == "history")
     anom = next(t for t in traces if str(t.get("name", "")).lower() == "anomaly")
-    hist_x = set(int(v) for v in hist.get("x", []))
+    hist_x = {int(v) for v in hist.get("x", [])}
     anom_x = [int(v) for v in anom.get("x", [])]
     assert anom_x
     assert all(x in hist_x for x in anom_x)
diff --git a/tests/unit/test_refactor_regressions.py b/tests/unit/test_refactor_regressions.py
index 80d771f..24bf4af 100644
--- a/tests/unit/test_refactor_regressions.py
+++ b/tests/unit/test_refactor_regressions.py
@@ -113,7 +113,7 @@ def test_get_dataframe_for_year_index_uses_real_years_not_epoch_ns(tmp_path):
 
     assert df is not None
     if isinstance(df.index, pd.DatetimeIndex):
-        years = set(int(y) for y in df.index.year.tolist())
+        years = {int(y) for y in df.index.year.tolist()}
         assert years == {2000, 2001, 2002}
     elif 'year' not in [str(c).lower() for c in df.columns]:
         # Year was absorbed into index by datetime inference ΓÇö check index values
